{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": { "value": "${AZURE_ENV_NAME}" },
    "location": { "value": "${AZURE_LOCATION}" },
    "principalId": { "value": "${AZURE_PRINCIPAL_ID}" },
    "deploymentProfile": { "value": "${DEPLOYMENT_PROFILE=poc}" },
    "fabricSqlServer": { "value": "${FABRIC_SQL_SERVER}" },
    "fabricSqlDatabase": { "value": "${FABRIC_SQL_DATABASE}" },
    "allowedIpRanges": { "value": "${ALLOWED_IP_RANGES}" },
    "allowedClientAppIds": { "value": "${ALLOWED_CLIENT_APP_IDS}" }
  }
}
